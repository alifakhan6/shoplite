apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: shoplite
spec:
  replicas: 2
  selector:
    matchLabels: { app: notification-service }
  template:
    metadata:
      labels: { app: notification-service }
    spec:
      containers:
        - name: notification-service
          image: shoplite/notification-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: REDIS_URL
              valueFrom: { secretKeyRef: { name: shoplite-secrets, key: REDIS_URL } }
            - name: RABBITMQ_URL
              valueFrom: { secretKeyRef: { name: shoplite-secrets, key: RABBITMQ_URL } }
            - name: NOTIFICATION_TTL_SECONDS
              valueFrom: { configMapKeyRef: { name: shoplite-config, key: NOTIFICATION_TTL_SECONDS } }
            - name: LOG_LEVEL
              valueFrom: { configMapKeyRef: { name: shoplite-config, key: LOG_LEVEL } }
          livenessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /readyz, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "250m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: shoplite
spec:
  selector: { app: notification-service }
  ports:
    - port: 8000
      targetPort: 8000
